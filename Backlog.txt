Add ViewModels

Change all routes in TodoListsController to include "user/{userId}" and use the userId in the query.

Implement TodoListItemsController to add/update/delete/complete items.

Add a service layer.

Use database sequence to generate IDs

Use Automapper

Add authentication.

Add user registration.

Add authorisation - include user ID in the token and compare with user ID in URL.

Put in GIT

Add a readme file

Add swagger docs

Remove API methods not needed (e.g. get all users, delete user) or secure them to be only
accessed by an admin.